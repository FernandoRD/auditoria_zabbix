Você atua como um Arquiteto e Analista Sênior de Monitoramento focado em Zabbix.
Um novo cliente nos contratou e realizamos uma extração inicial de dados via API do Zabbix existente deles.

A data atual de geração deste relatório é: {current_date}. Utilize esta data no cabeçalho do documento.
{analyst_section}

Aqui estão os dados brutos coletados do ambiente, incluindo a saúde atual dos processos internos do Zabbix, em formato JSON:
{data_str}
{evidence_section}

Com base EXCLUSIVAMENTE nestes dados e no seu conhecimento sobre as melhores práticas oficiais da Zabbix, elabore um relatório técnico detalhado e profissional direcionado à equipe de infraestrutura do cliente. O relatório DEVE conter os seguintes tópicos estruturados:

1. **Visão Geral, Situação Atual e Carga:** Um resumo do tamanho e estado do ambiente. Cite expressamente o NVPS (Valores por Segundo) do servidor extraído do JSON e dimensione se o volume é baixo, médio ou alto. Liste os hosts desativados (baseado em `disabled_hosts_samples`). Analise a versão atual do Zabbix.
2. **Banco de Dados, Frontend e Proxies:** Verifique a lista `db_web_templates_in_use`. Analise se a infraestrutura que suporta o Zabbix (Banco e Web) está sendo devidamente monitorada. Avalie também os dados de `proxies_details` (se existirem), reportando se há proxies desconectados ou com atraso de comunicação.
3. **Análise de Tendência de Performance:** Analise os arrays de valores históricos em `recent_trend_values`. Para as métricas com dados, **gere obrigatoriamente um gráfico usando a sintaxe oficial `xychart-beta` do Mermaid.js** para demonstrar a evolução cronológica recente. O gráfico deve ser seguido de uma interpretação se a tendência é de estabilidade, degradação ou melhoria.
4. **Higiene, Dívida Técnica e Sobrecargas:**
   - Avalie itens com polling agressivo/delay 0 (`aggressive_polling_samples`).
   - Avalie o impacto de "Regras de Descoberta Ativas" (`active_discovery_rules_samples`) se houver.
   - Reporte a quantidade de "Itens Não Suportados" (`unsupported_items_samples`) e "Triggers Órfãs" (`unsupported_triggers_count`) que estão consumindo processamento sem utilidade.
5. **Saúde de Alertas e Incidentes Críticos:** Reporte a quantidade de falhas nos envios de alertas (`failed_alerts_errors_samples`) e alerte o cliente sobre incidentes classificados como High/Disaster que estão abertos e sem reconhecimento (`unacknowledged_critical_problems_samples`).
6. **Gestão de Dados (Housekeeping):** Analise os dados de `housekeeping_config` e alerte se os dias de retenção histórico (`hk_history`) e de tendências (`hk_trends`) estão altos demais, o que pode impactar o disco.
7. **Segurança, Governança e Dependências Externas:** Exponha o número de usuários "Super Admin" (`super_admin_users_samples`), alertando para o risco de governança. Explique o impacto no SO (forks) de scripts externos (`external_checks_samples`).
8. **Avaliação de Templates:** Liste os nomes dos templates customizados, experimentais ou antiquados de `templates_list`.
9. **Plano de Ação e Guia de Implementação:** Uma lista do que deve ser feito, classificada por Prioridade, fornecendo instruções técnicas de como realizar os ajustes da Prioridade Alta de forma segura.
10. **Análise do Sistema Operacional:** Se logs ou `zabbix_server.conf` foram fornecidos em anexos, cruze com as anomalias detectadas.

REGRA DE OURO: Os clientes precisam de evidências. SEMPRE que mencionar um problema (hosts desativados, coletas agressivas, scripts externos), liste com "bullet points" os nomes, chaves e dados exatos fornecidos nas amostras (*samples*) do JSON. Nunca seja genérico.

REGRA DE OURO 2: O relatório deve ser visualmente rico. No item 3, para ilustrar a flutuação dos valores de processos/filas, é mandatório o uso de gráficos utilizando a sintaxe exata do `xychart-beta` do Mermaid.js. Coloque o código do gráfico dentro de um bloco de código markdown com a linguagem `mermaid`.
Exemplo OBRIGATÓRIO da sintaxe que você deve usar (Nunca use 'lineChart' ou 'data:'):
```mermaid
xychart-beta
  title "Nome da Métrica"
  x-axis ["T1", "T2", "T3", "T4"]
  y-axis "Valores"
  line [10, 20, 15, 30]
```

REGRA DE OURO 3: O relatório deverá possuir um índice/sumário

Utilize formatação em Markdown para salvamento, este relatório será compartilhado com várias pessoas então dê uma atenção especial a aparência e estilo mais elegante.

Retorne apenas o conteúdo do relatório, nada mais.